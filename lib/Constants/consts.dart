import 'package:flutter/material.dart';

var backgroundColor = Colors.black;

const welcome = "Welcome Aman,";
const appName = 'Collage Screen';

const margin = EdgeInsets.symmetric(horizontal: 4, vertical: 4);
